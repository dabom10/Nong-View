# Ultra-minimal requirements for Render.com - avoiding all Rust compilation
# Using older FastAPI that works with pydantic v1
fastapi==0.68.0
uvicorn==0.15.0
pydantic==1.10.13

# Only absolute essentials
python-multipart==0.0.5