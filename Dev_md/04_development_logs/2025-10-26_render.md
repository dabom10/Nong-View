# Render.com 설정 내역

  📋 기본 서비스 설정

  Web Service

  Name: nong-view-api
  Environment: python
  Plan: starter (무료 플랜 - 월 750시간)
  Branch: main
  Build Command: chmod +x build.sh && ./build.sh
  Start Command: chmod +x start.sh && ./start.sh

  🔧 Build Command 내역 (build.sh)

 !/bin/bash
   1. 시스템 패키지 업데이트
  apt-get update

   2. 빌드 도구 설치
  apt-get install -y build-essential g++ gcc libc6-dev pkg-config

   3. GDAL 지리정보 라이브러리 설치
  apt-get install -y gdal-bin libgdal-dev libproj-dev proj-data proj-bin

   4. Rust 툴체인 설치 (/tmp 디렉토리 사용)
  export CARGO_HOME=/tmp/cargo
  export RUSTUP_HOME=/tmp/rustup
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

   5. Python 의존성 설치 (다단계 fallback)
  pip install --upgrade pip setuptools wheel

   단계별 시도:
   - requirements.txt (FastAPI 0.68.0 + pydantic 1.10.13)
   - requirements-emergency.txt (최소한)
   - requirements-bare.txt (Starlette만)
   - 수동 설치 (uvicorn + starlette)

   6. 디렉토리 생성
  mkdir -p /tmp/uploads /tmp/crops /tmp/exports

  🎯 Start Command 내역 (start.sh)

  !/bin/bash
   1. 환경변수 설정
  export GDAL_CONFIG=/usr/bin/gdal-config
  export GDAL_DATA=/usr/share/gdal
  export PROJ_LIB=/usr/share/proj

   2. 디렉토리 생성
  mkdir -p /tmp/uploads /tmp/crops /tmp/exports

   3. API 서버 시작 (단계별 fallback)
  cd api

   시도 순서:
   1. main_bare.py (Starlette만 - 최소)
   2. main_emergency.py (FastAPI 최소)
   3. main_simple.py (FastAPI 확장)
   4. main.py (전체 기능)

  uvicorn [선택된API]:app --host 0.0.0.0 --port $PORT --workers 1

  🌍 환경변수 설정

  필수 환경변수

  PYTHON_VERSION=3.10.12
  ENVIRONMENT=production
  PORT=[Render가 자동 할당]

  지리정보 설정

  GDAL_CONFIG=/usr/bin/gdal-config
  GDAL_DATA=/usr/share/gdal
  PROJ_LIB=/usr/share/proj

  API 설정

  API_V1_STR=/api/v1
  PROJECT_NAME=Nong-View API

  파일 저장소

  UPLOAD_PATH=/tmp/uploads
  CROP_PATH=/tmp/crops
  EXPORT_PATH=/tmp/exports

  보안 설정

  SECRET_KEY=[자동 생성]
  ACCESS_TOKEN_EXPIRE_MINUTES=1440

  CORS 설정

  ALLOWED_HOSTS=*
  CORS_ORIGINS=https://your-frontend-domain.com,http://localhost:3000

  🗄️ 데이터베이스 설정

  PostgreSQL

  Name: nong-view-db
  Plan: free (1GB 저장소)
  Database: nongview
  User: nongview_user
  Extensions: postgis, uuid-ossp

  Redis

  Name: nong-view-redis
  Plan: free (25MB)
  Policy: allkeys-lru

  📊 서비스 URL

  메인 서비스: https://nong-view.onrender.com

  주요 엔드포인트:
  - / - 루트 (상태 확인)
  - /health - 헬스 체크
  - /test - 테스트 엔드포인트
  - /api/docs - API 문서 (FastAPI 자동 생성)

  현재 다단계 fallback 시스템으로 구성되어 있어, 의존성 문제가 발생해도 최소한 Starlette 기반 API는 동작하도록 설계되었습니다.

  ---

  ● ✅ Render.com 설정 가이드 완성!

  Dev_md/setup/ 폴더에 완성된 설정 가이드를 만들었습니다:

  📁 생성된 파일들:

  1. setup/README.md - 전체 개요 및 빠른 시작
  2. setup/postgresql-setup.md - PostgreSQL 상세 설정 (⚠️ 2개 제한!)
  3. setup/render-services-setup.md - 전체 서비스 배포 가이드

  🔑 핵심 내용:

  PostgreSQL 설정 방법:

  - 무료 플랜 제한: 계정당 최대 2개만 생성 가능
  - 권장 사용: 1개는 개발용, 1개는 프로덕션용
  - PostGIS 설치: 수동 설치 필요 (CREATE EXTENSION postgis;)
  - 스키마 설계: 이미지, 분석, 크롭, 내보내기 테이블

  전체 배포 순서:

  1. GitHub 리포지토리 준비
  2. PostgreSQL 생성 (2개 제한 고려!)
  3. Redis 생성
  4. Web Service 생성 (render.yaml 사용)
  5. 환경변수 설정
  6. PostGIS 수동 설치

  환경변수 체크리스트:

  - DATABASE_URL (자동 연결)
  - REDIS_URL (자동 연결)
  - SECRET_KEY (Generate Value 사용)
  - ENVIRONMENT=production
  - CORS, 파일 경로, GDAL 설정 등

  이제 PostgreSQL 2개 제한을 고려하여 효율적으로 Render.com에 배포할 수 있습니다! 🚀
